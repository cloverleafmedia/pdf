// Shared between PDFPage's redraw() (committed marks) and the live-drag
// preview in the matching tool hook, so a drag looks the same before and
// after mouseup.
export const REDACTION_FILL = 'rgba(0,0,0,0.55)'
export const SHAPE_STROKE = '#8b5cf6'
